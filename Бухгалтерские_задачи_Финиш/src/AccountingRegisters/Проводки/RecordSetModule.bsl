
Процедура ПередЗаписью(Отказ, РежимЗаписи)
	
	Если Количество() = 0 Тогда
		Возврат;
	КонецЕсли;
	
	КроссКурс = ОбщийМодульНаСервереВызовСКлиента.ПересчитатьИзВалютыВВалюту(
	,Константы.ВалютаМСФО.Получить(),10000,Получить(0).Период);
	
	Для каждого Запись Из ЭтотОбъект Цикл
	
		 Запись.СуммаМСФО = Запись.Сумма * КроссКурс / 10000;
	
	КонецЦикла;
	
	
КонецПроцедуры
